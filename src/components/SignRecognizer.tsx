// SignRecognizer component
// Captures webcam feed and runs MediaPipe Hands landmark detection.
// Streams hand keypoints to the sign-classifier and displays
// real-time recognition feedback with confidence scores.

export default function SignRecognizer() {
  return <div className="relative" />;
}
