// ChatPanel component
// Real-time text chat sidebar connected via Azure SignalR Service.
// Displays translated captions, sign language gloss output, and
// allows participants to type messages with content safety filtering.

export default function ChatPanel() {
  return <aside className="w-80 bg-slate-800 h-full" />;
}
