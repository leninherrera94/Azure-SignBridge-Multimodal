// TranscriptionOverlay component
// Renders live speech-to-text captions as an overlay on the video feed.
// Supports configurable position, font size, color contrast, and
// a scrollable history of the full meeting transcript.

export default function TranscriptionOverlay() {
  return (
    <div className="absolute bottom-4 left-0 right-0 text-center text-white bg-black/60 p-2 rounded" />
  );
}
